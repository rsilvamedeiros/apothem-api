# identity

**Status:** Placeholder — no code yet

Owns user/principal identity: authentication, sessions, credentials. Not authorization/permissions (see `../authorization`), not organization membership (see `../organizations`).

Reference docs (`apothem-ai/docs/`):
- `03-domain/users-memberships.md`
- `08-security/authentication-authorization-rbac.md`

Stack baseline: self-hosted OIDC (Auth.js/NextAuth or Lucia) — see `apothem-ai/docs/adr/009-zero-cost-initial-stack.md`.
