# infrastructure/http

**Status:** Placeholder — no code yet

HTTP server/framework wiring: request parsing, error-to-response mapping, correlation IDs, OpenAPI spec generation/serving. Transport concerns only — no business rules here (those live in module `application`/`domain` layers).

Reference: `apothem-ai/docs/10-api/api-guidelines.md`.
