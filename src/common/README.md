# common

**Status:** Placeholder — no code yet

Cross-module utilities with no business rules and no tenant-specific logic: shared types, error base classes, validation helpers, generic result/either wrappers. If something here starts encoding business rules, it belongs in a module instead.
