# infra/docker

**Status:** Placeholder — no `docker-compose.yml` yet

Will hold the local Docker Compose setup: PostgreSQL + pgvector, Redis, MinIO — the zero-cost local dev baseline from `apothem-ai/docs/adr/009-zero-cost-initial-stack.md`. Goal: `docker compose up -d` followed by application bootstrap, no cloud account required for day-to-day development.
