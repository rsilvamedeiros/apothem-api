# infra/fly

**Status:** Placeholder — no `fly.toml` yet

Fly.io deploy configuration for the backend (API + workers), chosen for its free allowance and support for long-running containers/workers — see `apothem-ai/docs/adr/009-zero-cost-initial-stack.md` for why Fly.io over Railway/Render.
