# database

**Status:** Placeholder — no code yet

Seed/demo fixture data for local development and CI (e.g. a demo organization/workspace/agent). Not the migrations themselves — those live in `../migrations`.
